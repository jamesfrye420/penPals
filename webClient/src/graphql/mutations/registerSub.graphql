mutation registerSub($options: SubDetailsInput!) {
  registerSub(options: $options) {
    errors {
      field
      message
    }
    sub {
      id
      name
      subIdentifier
      createdAt
    }
  }
}
